server {
    listen 80;
    server_tokens off;

    location /api/ {
        proxy_set_header Host $http_host;
        proxy_pass http://backend:8000/api/;  # Исправлен порт на 8000
    }
    
    location /admin/ {
        proxy_set_header Host $http_host;
        proxy_pass http://backend:8000/admin/;  # Исправлен порт на 8000
    }

    location / {
        root /static/frontend;  # Используем root вместо alias
        try_files $uri $uri/ /index.html;
        index index.html;
    }

    location /backend_static/ {
        alias /static/collected_static/;
    }

    location /media/ {
        alias /media/;
    }

    # Явная обработка favicon.ico
    location = /favicon.ico {
        alias /static/frontend/favicon.ico;
        access_log off;
        log_not_found off;
    }
}